[tox]
envlist = py35,py36,py37
# skipsdist=True

[testenv]
# install pytest in the virtualenv where commands will be executed
# passenv = *
setenv =
  DJANGO_SETTINGS_MODULE=test_setup.settings
deps = -rtest_setup/requirements.txt
commands =
  pytest -rs
